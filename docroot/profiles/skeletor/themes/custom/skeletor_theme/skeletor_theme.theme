<?php
/**
 * @file
 * Bootstrap sub-theme.
 *
 * Place your custom PHP code in this file.
 */ 

/**
 * Include preprocess functions
 */
foreach (glob(dirname(__FILE__) . "/preprocess/*.theme") as $filename) {
  include_once $filename;
}
